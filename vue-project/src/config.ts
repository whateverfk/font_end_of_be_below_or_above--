/**
 * Cấu hình hệ thống
 * Bạn có thể thay đổi Domain API tại đây để áp dụng cho toàn bộ dự án.
 */
export const API_CONFIG = {
  // Thay đổi URL này khi deploy hoặc kết nối với backend
  BASE_URL: 'http://127.0.0.1:8000',
  TIMEOUT: 5000,
}
